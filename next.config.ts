import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['i.pinimg.com', 'imageadress.com'], // puedes añadir más dominios aquí si usas otros
  },
};

export default nextConfig;
